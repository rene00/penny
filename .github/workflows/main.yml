name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: 
        - apt-get install -y python3 python3-dev python3-pip python3-setuptools libpython3-dev python3.6-dev
        - make build
