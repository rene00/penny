name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install python3
        run: apt-get install -y python3 python3-dev python3-pip python3-setuptools libpython3-dev python3.6-dev
      - name: build
        run: make build
