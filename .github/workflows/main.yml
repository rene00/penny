name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install python3
        run: sudo apt-get install -y python3 python3-dev python3-pip python3-setuptools libpython3-dev python3.6-dev
      - name: tox
        run: tox
